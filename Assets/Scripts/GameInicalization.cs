using UnityEngine;

public class GameInicalization : MonoBehaviour
{
    

    //private void Start()
    //{
    //    PolicyButtonUI[] buttons = FindObjectsOfType<PolicyButtonUI>();

    //    float totalIncome = 0f;
    //    float totalExpenses = 0f;

    //    foreach (var btn in buttons)
    //    {
    //        if (btn.policyData == null) continue;

    //        totalIncome += btn.policyData.income;
    //        totalExpenses += btn.policyData.cost;
    //    }

    //    GameManager.Instance.SetIncome(totalIncome);
    //    GameManager.Instance.SetExpensive(totalExpenses);

    //    Debug.Log($"[BOOTSTRAP] Income: {totalIncome}, Expenses: {totalExpenses}");
    //}

}
